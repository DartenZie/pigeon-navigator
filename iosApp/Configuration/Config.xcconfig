TEAM_ID=

PRODUCT_NAME=PigeonNavigator
PRODUCT_BUNDLE_IDENTIFIER=cz.miroslavpasek.pigeonnavigator.PigeonNavigator$(TEAM_ID)

CURRENT_PROJECT_VERSION=1
MARKETING_VERSION=1.0

// SQLDelight native-driver uses the system libsqlite3 at link time
OTHER_LDFLAGS = $(inherited) -lsqlite3
